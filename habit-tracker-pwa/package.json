{
  "name": "habit-tracker-pwa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 8787 --host",
    "start": "npm run build && vite preview --port 8787 --host"
  },
  "devDependencies": {
    "vite": "^5.2.11",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "dependencies": {
    "@noble/ciphers": "^2.2.0",
    "@noble/hashes": "^2.2.0"
  }
}
